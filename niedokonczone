#PALINDROM
/*
#include <stdio.h>
#include <random>
#include <iostream>

using namespace std;

int czypalindrom(string tekst) {
    for (int i = 0; i < tekst.length()/2; i++) {
        if(tekst[i]!=tekst[tekst.length() -1 -i]) {
            return false;
        }else return true;
    }
}

int main() {
    string tekst;
    cin >> tekst;
    cout << czypalindrom(tekst) << endl;
}
*/
